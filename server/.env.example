PORT=3001

# Database
DB_USER=postgres
DB_HOST=localhost
DB_PORT=5432
DB_NAME=rms_pro
DB_PASSWORD=

# JWT — REQUIRED. Generate with: node -e "console.log(require('crypto').randomBytes(64).toString('hex'))"
JWT_SECRET=

# CORS — comma-separated list of allowed origins (defaults to localhost in dev)
# ALLOWED_ORIGINS=https://yourdomain.com,https://www.yourdomain.com
